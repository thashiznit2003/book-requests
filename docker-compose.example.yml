services:
  book-requests:
    image: book-requests:latest
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    env_file: .env
    environment:
      NODE_ENV: production
    volumes:
      - ./data:/app/data
    restart: unless-stopped
